[project]
name = "civic-agentcy"
version = "0.2.0"
description = "Policy research and analysis using CrewAI multi-agent framework"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai>=0.152.0",
    "python-dotenv",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
